# http => https
server {
  listen 80;
  server_name slots.daniellacos.se;

  return 301 https://slots.daniellacos.se$request_uri;
}

server {
  listen [::]:443 ssl http2;
  server_name slots.daniellacos.se;

  proxy_pass "https://idea-slot-machine.daniellacosse.now.sh$request_uri";
}
